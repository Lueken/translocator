# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Mobile icon sets generated by `tauri icon` (this is a desktop-only app)
src-tauri/icons/android/
src-tauri/icons/ios/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local signing config. Machine-specific paths and an Azure account name, kept
# out of the repo so a contributor's `tauri build` never fails on a signtool or
# dlib they do not have. Recreate from docs/releasing.md.
*.local.json
*.local.cmd
